1) create and login into server

2) off Password Authentication
	sudo vim /etc/ssh/sshd_config 
	PasswordAuthentication no

3) setup firewall (uwf)
	for allow porst
		sudo ufw allow 21
		sudo ufw allow 22
		sudo ufw allow 80
	for enable firewall
		sudo ufw enable
	for check the status
		sudo ufw statu

4) setup aws firewall
	for setup aws firewall
		Security -> Security groups -> Edit inbound rules
		Add rules
			21: Custom TCP (FTP)
			22: SSH
			80: HTTP
5) setup fail2ban
	download fail2ban
		sudo apt install fail2ban
	run fail2ban
		sudo systemctl start fail2ban		
	check fail2ban
		sudo systemctl status fail2ban
	change configuration
		vim /etc/fail2ban/jail.local
6) apache2
	dowload apache2
		sudo apt install apache2
	start apache2
		sudo systemctl enable apache2
		sudo systemctl start apache2
	test apache2
		curl http://localhost
		or
		open in browser http://public_ip

